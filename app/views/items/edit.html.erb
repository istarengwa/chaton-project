<% content_for :title, "Modifier le chaton 📝" %>

<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm border-0">
        <div class="card-body">
          <h2 class="card-title mb-4 text-center">🛠️ Modifier <%= @item.title %></h2>
          <%= render "form", item: @item %>

          <div class="d-flex justify-content-between mt-4">
            <%= link_to "👁️ Voir le chaton", @item, class: "btn btn-outline-primary" %>
            <%= link_to "← Retour à la boutique", items_path, class: "btn btn-outline-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

