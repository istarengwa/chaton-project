<% content_for :title, @item.title %>

<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-lg border-0">
        <%= image_tag @item.image_url, alt: @item.title, class: "card-img-top", style: "object-fit: cover; height: 400px;" %>

        <div class="card-body">
          <h1 class="card-title"><%= @item.title %></h1>
          <p class="text-muted"><%= @item.description %></p>
          <h4 class="text-success"><%= number_to_currency(@item.price, unit: "€") %></h4>

          <div class="mt-4 d-flex gap-2">
            <%= link_to "Retour à la boutique", items_path, class: "btn btn-outline-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>